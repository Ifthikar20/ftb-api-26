# Copy to backend.hcl and fill in. Then:
#   terraform init -backend-config=backend.hcl
bucket         = "fetchbot-tfstate-<ACCOUNT_ID>"
key            = "prod/terraform.tfstate"
region         = "us-east-1"
dynamodb_table = "fetchbot-tflock"
encrypt        = true
