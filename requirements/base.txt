# Core Django
Django==5.1.*
djangorestframework==3.15.*
djangorestframework-simplejwt==5.3.*
django-cors-headers==4.4.*
django-filter==24.*
django-environ==0.11.*

# Database
psycopg2-binary==2.9.*

# Cache & Celery
redis==5.0.*
django-redis==5.4.*
celery==5.4.*
django-celery-beat==2.7.*
django-celery-results==2.5.*

# WebSockets
channels==4.1.*
channels-redis==4.2.*
daphne==4.1.*
# Pin autobahn off the 25.x line: 25.10.1 is a broken release on PyPI
# (missing setup.py / pyproject.toml) and 25.12 forces a hard py-ubjson
# build that fails on Python 3.12 / Apple Silicon. 24.4.x ships wheels
# for our target platforms and satisfies daphne 4.1's >=22.4.2 floor.
autobahn==24.4.*

# API Documentation
drf-spectacular==0.27.*

# Authentication & Security
django-axes==7.0.*
django-otp==1.5.*
cryptography==42.*
# Google id_token verification for the SSO lane. Already present
# transitively via google-generativeai — pinned here because
# apps/accounts imports it directly now.
google-auth==2.*
# Microsoft Entra id_token verification (JWKS fetch + JWT decode) for
# the Entra SSO lane in apps/accounts.
authlib==1.6.*
# DNS TXT lookups for org domain verification (pure python).
dnspython==2.*

# File Storage
django-storages==1.14.*
boto3==1.35.*

# Health Checks
django-health-check==3.18.*

# Logging
django-structlog==8.*
structlog==24.*

# HTTP Requests
# httpx floor set by polar-sdk (>=0.28.1); nothing in-repo imports httpx
# directly, it only backs the anthropic/openai SDKs. httpx 0.28 removed
# the `proxies` kwarg, so the SDK floors below are the first releases
# compatible with it (anthropic 0.40, openai 1.55.3) — older pins fail
# every call with "unexpected keyword argument 'proxies'".
requests==2.32.*
httpx==0.28.*

# AI / LLM
anthropic==0.40.*
# openai >= 2.x is required for client.responses (the web_search tool the
# gpt4/grok providers use for cited answers); 1.56 silently fell back to
# plain, source-less chat completions. 2.x keeps the same chat.completions
# and embeddings surface this repo uses, and stays httpx-0.28-compatible.
openai==2.54.*
google-generativeai==0.8.*


# Usage metering (Polar.sh event ingestion + meters)
polar-sdk==0.32.*

# Task utilities
celery[redis]==5.4.*

# Utilities
Pillow==10.*
python-slugify==8.*
pytz==2024.*
beautifulsoup4==4.*
pytrends==4.*
tldextract==5.*

# Production
gunicorn==23.*
whitenoise==6.7.*
sentry-sdk[django]==2.*
