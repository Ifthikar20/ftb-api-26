# LLM Ranking — TODO

## Open

- [ ] Run `python manage.py migrate llm_ranking` to apply `0012_brand_strengths_and_smoothed_rate` and `0013_region_and_citation_countries`
- [ ] Run `pytest apps/llm_ranking/tests/test_plackett_luce.py apps/llm_ranking/tests/test_services.py` against a real DB
- [ ] Run `bash scripts/deploy.sh eval` against existing audits to capture baseline NDCG@5 / MRR for the PR description
- [ ] LambdaMART learning-to-rank model trained on per-prompt position data — replaces the static rank component of `compute_overall_score`
- [ ] Cross-provider agreement KPI (Krippendorff's alpha or simple agreement ratio per prompt)
- [ ] Calibrate `confidence_score` via Platt scaling / isotonic regression once a labeled set exists
- [ ] Plackett-Luce posterior uncertainty (bootstrap or Bayesian PL) so the rankings table can show CI bars per brand

## In Progress

_(none)_

## Done

- [x] LLM visibility audit models (queries, results, rankings)
- [x] Multi-provider query execution
- [x] ARCHITECTURE.md documentation
- [x] Add scheduling for periodic LLM visibility audits
- [x] Add comparison across LLM providers (GPT vs Claude vs Gemini)
- [x] Add trend visualization data endpoint
- [x] Plackett-Luce brand-strength ranking (`services/plackett_luce.py`) wired into `finalise_audit`
- [x] Beta-Binomial smoothed mention rate replacing raw proportion in the score formula
- [x] Wilson 95% CI on mention rate exposed in the API + dashboard
- [x] Offline NDCG@5 / MRR evaluation harness (`bash scripts/deploy.sh eval`)
- [x] Frontend Rankings table sorts by Plackett-Luce strength when present
- [x] Frontend KPI shows smoothed value + CI sub-line + confidence dot
- [x] Per-user RAG knowledge base (`apps/rag`) wired into `generate_prompts` + per-cell retrieval + audit-context self-ingest "learning" loop
- [x] Geographic regions: prompt flavoring + Perplexity `user_location` routing + ccTLD-based citation country attribution + dashboard "Citation Footprint by Country" card

## Region follow-ups (P1+)

- [ ] Per-region small-multiples on the Competitor Visibility chart (one line per region)
- [ ] "Run audit in all regions" batch action that fan-outs N audits in parallel and stitches a regional comparison view
- [ ] Replace ccTLD heuristic with MaxMind GeoLite2 for accurate hosting-country attribution (~70MB binary, free with attribution)
- [ ] Proxy-routed scraping (BrightData / Oxylabs regional endpoints) so the RAG knowledge base reflects region-specific web content
